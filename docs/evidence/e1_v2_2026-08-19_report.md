# E1 v2 — Đo lại sau khi hạ A6/A4 xuống nhóm B (chạy 2026-08-19)

Số liệu thô: [`e1-v2-2026-08-19.json`](e1-v2-2026-08-19.json), report thô:
[`e1-v2-2026-08-19-report.json`](e1-v2-2026-08-19-report.json).
Lượt đo trước (không đạt, đã chẩn đoán): [`e1_v2_2026-08-18_report.md`](e1_v2_2026-08-18_report.md).

**Kết luận: `decision_consistency = 1,00` ≥ 0,90 → ĐẠT. Cổng sang Gold v2 mở.**

## Truy vết

| Thành phần | Giá trị |
|---|---|
| HEAD lúc chạy | `a6ba866a354d584b589a15266cf1a05a6da68b53` |
| `release_source_commit` | commit hạ A4 (`8b4036f`) qua freeze `a6ba866` |
| Mẫu | `G-001`..`G-010`, mỗi bài 5 lượt = 50 lượt |
| Chi phí thật | **$3,21** (một lượt sạch, không ghép nối) |

## Thay đổi so với lượt trước (2026-08-18, không đạt)

Giữa hai lượt đo, đã hạ quyền chặn của `A6` (commit `eb5898e`) và `A4`
(commit `8b4036f`) xuống nhóm B — cả hai giữ nguyên tên mã, chỉ không còn
tự mình đẩy quyết định sang `rejected` một mình nữa. Không sửa gì khác.

## Kết quả từng bài

| Bài | Quyết định phổ biến | `mode_agreement` | σ `final_score` |
|---|---|---|---|
| G-001 | needs_revision | **1,00** | 1,33 |
| G-002 | needs_revision | **1,00** | 0,79 |
| G-003 | needs_revision | **1,00** | 4,59 |
| G-004 | needs_revision | **1,00** | 2,38 |
| G-005 | needs_revision | **1,00** | 2,11 |
| G-006 | needs_revision | **1,00** | 1,43 |
| G-007 | needs_revision | **1,00** | 0,70 |
| G-008 | needs_revision | **1,00** | 1,88 |
| G-009 | needs_revision | **1,00** | 1,14 |
| G-010 | rejected | **1,00** | 0,00 |

**10/10 bài hoàn toàn ổn định** — đúng 4 bài từng dao động (`G-003`, `G-006`,
`G-007`, `G-008`) giờ đều `mode_agreement = 1,00`.

## Chỉ số tổng hợp

| Đại lượng | Giá trị | Ngưỡng | Đạt? |
|---|---|---|---|
| `decision_consistency` | **1,00** | ≥ 0,90 | ✅ |
| σ `final_score` (chẩn đoán, không quyết định) | 1,64 | < 2 | ✅ |

## Ghi chú diễn giải — không được trích quá đà

- **n=10, mẫu nhỏ.** `decision_consistency = 1,00` không loại trừ khả năng
  còn tiêu chí khác (ngoài A6/A4) dao động nhưng chưa xuất hiện trong đúng
  10 bài này — giống cách trần Kappa 1,000 của test-retest nhãn (mục 8.3)
  từng phải kèm giới hạn tương tự.
- **Chưa kiểm được dao động dạng `needs_revision` ↔ `publish`.** Cả 10 bài
  đều rơi vào `needs_revision`/`rejected`, không có bài nào gần `publish` —
  nên chưa biết A6/A4 (nhóm B) có gây lật quyết định ở biên đó không.
- σ `final_score` tăng nhẹ so với lượt trước (1,60 → 1,64) — không đáng kể,
  vẫn đạt xa ngưỡng < 2, và `final_score` chỉ mang tính mô tả, không quyết
  định.

## Việc kế tiếp

E1 đã đạt — theo đúng thứ tự bắt buộc, **Gold v2** là bước tiếp theo (chỉ
sau khi E1 đạt). Chi phí ước tính ~$1,9-2,2 (dựa theo đơn giá quan sát được
ở lượt này, cao hơn ước tính preflight cũ do full 4-agent chạy sạch).
`scoring.yaml.meta.calibrated` không đổi.
