langgraph>=1.2.5
anthropic>=0.40.0
python-dotenv>=1.0.0
requests>=2.31.0
sentence-transformers>=3.0.0

# Client goi embedding qua HuggingFace Space thay vi nap BGE-M3 local
# (RemoteEmbedder trong src/embeddings.py, dung khi dat EMBEDDING_SPACE_URL).
gradio_client>=1.0.0

# Kho vector: Postgres + pgvector (thay Chroma tu 2026-08-05). Xem
# multiagent/docker-compose.yml va src/db.py.
psycopg[binary]>=3.2.0

# PyYAML phai khai bao TUONG MINH. Truoc day no khong co trong file nay ma
# src/config.py van chay duoc, vi chromadb keo no theo. Go chromadb ra la
# config.load() te - ma do la duong di cua MOI lan cham. Dung de phu thuoc an
# ma khong ai thay, cho toi luc no bien mat.
PyYAML>=6.0

# Service HTTP nhan job tu Drupal (spec 2026-08-07). uvicorn la server ASGI
# chay app cua fastapi - khong co no thi `python -m uvicorn` te ngay.
fastapi>=0.115
uvicorn>=0.30

# Platform Admin MVP: render giao dien server-side, nhan HTML form va bam mat
# khau Argon2id. Giu range major de lan cai dat sau khong tu dong vo API.
Jinja2>=3.1,<4
python-multipart>=0.0.9,<1
argon2-cffi>=23.1,<26
